name: Pull Request validation

on: [pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

extends:
  - .github/workflows/validate-contracts-template.yml
